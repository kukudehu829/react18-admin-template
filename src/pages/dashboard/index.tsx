import { Button } from "antd";

const Dashboard: React.FC = () => {
  const addRoutes = () => {
    
  }


  return (
    <div>
      看板
      <Button onClick={addRoutes}>点击新增动态路由</Button>
    </div>
  )
}

export default Dashboard